server:
  port: 8080

spring:
  application:
    name: abcmsc-consumer-depart

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka

  instance:
    metadata-map:    # 添加自定义的元数据
      host-mark: running-host
